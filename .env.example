# AWS Cognito Configuration
# Required for user authentication

# Cognito User Pool ID (e.g., us-east-1_xxxxxxxxx)
VITE_COGNITO_USER_POOL_ID=us-east-1_bT420YtZu

# Cognito User Pool Web Client ID
VITE_COGNITO_CLIENT_ID=2msbcnhq522rp53i2kib091u76

# AWS Region where your Cognito resources are deployed
VITE_COGNITO_REGION=us-east-1

# Cognito Identity Pool ID (required for OAuth2 desktop authentication)
# This is used to exchange Cognito tokens for temporary AWS credentials
# to access DynamoDB tables for the OAuth2 flow
VITE_IDENTITY_POOL_ID=us-east-1:xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# OAuth2 Configuration
# DynamoDB table names for OAuth2 PKCE flow (desktop authentication)

# Table for storing OAuth2 sessions (10 minute TTL)
VITE_OAUTH2_SESSIONS_TABLE=fount-oauth2-sessions

# Table for storing authorization codes (5 minute TTL)
VITE_AUTH_CODES_TABLE=fount-auth-codes

# Instructions:
# 1. Copy this file to .env
# 2. Replace the placeholder values with your actual AWS resource IDs
# 3. Never commit the .env file to version control
# 4. The .env file is already in .gitignore
